# Geem Workspace Web — local environment

# FastAPI base URL (browser-reachable)
VITE_API_URL=http://localhost:8000

# Root domain for workspace subdomain UX (Phase 1+)
# Local DX may use header/slug fallback instead of real DNS.
VITE_ROOT_DOMAIN=localhost
